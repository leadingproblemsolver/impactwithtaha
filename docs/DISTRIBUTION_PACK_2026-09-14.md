# Distribution pack — 2026-09-14

This pack exists to create external evidence, not to announce a redesign.

## Release gate

Do **not** distribute `impactwithtaha.vercel.app` as the refreshed canonical portfolio until GitHub issue #9 is closed and the exact merged SHA passes the existing production HTTP + Chromium smoke gates.

The construction PR can be distributed now because its public source and evidence boundary already exist independently.

---

## 1. Highest-information ask — construction engineer / planner

### DM / WhatsApp / email

I built a narrow deterministic handoff for one planning cycle:

current P6 activities + relationships + previous schedule snapshot + QA/document requirements → 90-day readiness + schedule changes + source-linked impact.

The synthetic path is green at 34 tests. I do **not** want to add more features before it survives a real planning cycle.

If you can anonymize the four normal files, I’ll run the current pipeline unchanged and return the row-level output. You only need to mark disputed rows **ACCEPT / CORRECT / REJECT** and add a short note where needed.

No adoption commitment needed. The purpose is to find where the model is wrong against real work.

Public proof / exact boundary:
https://github.com/leadingproblemsolver/reality-ontology-runtime/pull/13

### Required receipt

`real four-file input -> unchanged run -> engineer ACCEPT/CORRECT/REJECT -> preserved corrections`

Nothing else outranks this for the construction slice.

---

## 2. X — construction validation request

Built a deterministic construction-planning handoff:

P6 activities + relationships + prior snapshot + QA/doc requirements → 90-day readiness, schedule changes, source-linked impact.

34 tests green. Still synthetic-only.

Looking for **1 planner/engineer** willing to anonymize one cycle and mark the output ACCEPT / CORRECT / REJECT.

https://github.com/leadingproblemsolver/reality-ontology-runtime/pull/13

### Success event

A qualified planner/engineer replies, opens the PR, supplies a cycle, introduces the right operator, or explicitly rejects the premise with a concrete reason.

---

## 3. LinkedIn — construction validation request

I’m testing a narrow construction-planning question rather than building another broad “AI for construction” demo:

Can current P6 activities, relationships, a previous schedule snapshot, and QA/document requirements be turned into a deterministic 90-day readiness review with source-linked change impact — without hiding uncertainty or inventing unsupported effects?

The current public slice produces:
- input preflight;
- 90-day readiness;
- schedule changes;
- source-linked change impact;
- a separate engineer review copy;
- a projection receipt.

The current synthetic path is reproducible and the public PR is green at 34 tests.

That is **not** proof it works on a real Primavera export or a live project.

So the next step is deliberately small: one anonymized real planning cycle, run unchanged, then row-level **ACCEPT / CORRECT / REJECT** from the engineer.

If you work directly with Primavera/P6 planning, construction lookaheads, QA/document readiness, or know someone who does, I’d value one real correction cycle far more than another feature request.

Public PR + evidence boundary:
https://github.com/leadingproblemsolver/reality-ontology-runtime/pull/13

---

## 4. Hiring evaluator — after portfolio production gate is green

Instead of a project list, I’ve organized my work as inspectable proof:

`messy workflow -> explicit state / constraint -> bounded mechanism -> hostile test -> external receipt`

The public surface keeps reproducible behavior, deployment, external interaction, and measured outcomes separate.

If you send the actual role/JD or one workflow the team owns, the Lens compiles the evidence most relevant to that context without upgrading the claim level.

Portfolio:
https://impactwithtaha.vercel.app/

Source:
https://github.com/leadingproblemsolver/impactwithtaha

### Ask

Give me one real workflow boundary or failure case that an intern / implementation engineer could safely own. I’ll start from the existing proof rather than proposing a speculative project.

---

## 5. Founder / operator — after portfolio production gate is green

I work best on workflows where the real state is scattered across tools, people, retries, or ambiguous handoffs.

My current pattern is:

`observe actual state -> make the constraint explicit -> implement the smallest bounded mechanism -> test failure paths -> hand it to the operator -> preserve the correction`

If you have one workflow that repeatedly becomes stale, ambiguous, manually reconciled, or unreliable around AI/automation, send me the smallest real example. I’ll tell you whether one of the existing mechanisms is relevant before proposing anything new.

https://impactwithtaha.vercel.app/

### Ask

One artifact, one broken handoff, or one state transition — not a broad product brief.

---

## 6. In-person / QR handoff

Use one sentence before showing any surface:

> I build small operational systems around places where the real state is ambiguous or unreliable. This page shows what is actually reproducible, what has external evidence, and what still needs a real operator test.

Then route by observer:
- employer -> `/lens` with actual JD/workflow;
- technical reviewer -> `/proof-map` / source PR;
- construction planner -> PR #13 directly;
- buyer/operator -> `/start` with one real boundary;
- collaborator -> one joint falsifiable test.

## Stop condition

Do not create more launch content until at least one of the above creates:
- real-input access;
- correction/rejection;
- technical review;
- interview/task;
- introduction;
- use/payment.

No response is also evidence; record channel, audience, message, timestamp, and outcome before changing the positioning.
