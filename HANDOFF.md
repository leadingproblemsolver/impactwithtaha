# YOUR ONLY ACTIONS AFTER RECEIVING THIS PACKAGE

## Required now
1. Unzip the package.
2. Open `index.html` locally and confirm your name/contact details are correct.
3. Publish the whole folder to ONE static host:
   - GitHub Pages, OR
   - Netlify Drop, OR
   - Vercel static deployment.
4. Return the live portfolio URL to ChatGPT.

## Optional but useful
5. If the specific repositories are already public, replace the `Add direct repository link` text in the proof pages with the exact repo URLs.
6. If they are private, do nothing yet. The included case studies are enough for the first opportunity-search pass.

## Do NOT do yet
- redesign the page;
- make a logo;
- record Loom videos;
- build another app;
- publish raw ZIPs/chats/datasets;
- rewrite the case studies;
- start broad LinkedIn/X content;
- create speculative projects.

Once you return the live URL, ChatGPT should immediately start the live opportunity qualification stage and produce the first pursuit packages.
